import { bootstrapGame } from './game/bootstrap.js';
export { createGameCore } from './game/core.js';

/**
 * Bootstraps the browser game with UI bindings and persistence wiring.
 * Consumers must call this manually to avoid side effects during import.
 */
export { bootstrapGame };
